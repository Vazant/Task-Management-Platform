package com.taskboard.userservice.infrastructure.config;

import org.springframework.context.annotation.Configuration;

/**
 * JPA configuration for the user service.
 *
 * <p>This configuration class serves as a marker for JPA-related configurations.
 * The actual JPA setup is handled by DatabaseConfig.
 *
 * @author Task Management Platform Team
 * @version 1.0.0
 * @since 1.0.0
 */
@Configuration
public class JpaConfig {
  // JPA configuration is handled by DatabaseConfig
  // This class serves as a marker for JPA-related configurations
}
